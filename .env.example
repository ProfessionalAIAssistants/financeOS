# Database
POSTGRES_USER=finance_admin
POSTGRES_PASSWORD=change_me_strong_password

# Firefly III
FIREFLY_APP_KEY=change_me_32_char_random_string_here
FIREFLY_TOKEN=           # Generate after first login: Profile > OAuth > Personal Access Tokens

# Sync Service
ENCRYPTION_KEY=          # 64 hex chars: openssl rand -hex 32
NODE_ENV=production
TZ=America/Denver        # Your local timezone (e.g. America/New_York, America/Chicago, America/Los_Angeles)

# AI (OpenAI)
OPENAI_API_KEY=          # sk-...

# Push Notifications (ntfy.sh)
NTFY_TOPIC=my-finance-alerts
NTFY_URL=http://ntfy:8080

# Property Value APIs
HOMESAGE_API_KEY=        # Sign up at homesage.ai
RENTCAST_API_KEY=        # Sign up at rentcast.io

# OFX Direct Connect
CHASE_CLIENTUID=         # Generate once: python -c "import uuid; print(uuid.uuid4())"
CHASE_USERNAME=          # Your Chase username
CHASE_PASSWORD=          # Your Chase password (encrypted at rest)

USAA_ACCESS_ID=          # From usaa.com/accessid
USAA_ACCESS_PIN=         # From usaa.com/accessid

# finance-dl (Selenium scraper credentials)
CAPITALONE_USERNAME=
CAPITALONE_PASSWORD=

MACU_USERNAME=
MACU_PASSWORD=

M1FINANCE_USERNAME=
M1FINANCE_PASSWORD=

# ── Auth / SaaS ──────────────────────────────────────────────────────────────
# JWT secret — generate with: openssl rand -hex 32
JWT_SECRET=change_me_generate_with_openssl_rand_hex_32
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=30d
APP_URL=https://your-tunnel-domain.example.com   # Public URL (Cloudflare Tunnel hostname)

# ── Stripe ────────────────────────────────────────────────────────────────────
# Create your products at https://dashboard.stripe.com/products
STRIPE_SECRET_KEY=sk_live_...           # or sk_test_... for testing
STRIPE_WEBHOOK_SECRET=whsec_...         # From Stripe Dashboard → Webhooks
STRIPE_PRO_PRICE_ID=price_...           # Monthly recurring price ID
STRIPE_LIFETIME_PRICE_ID=price_...      # One-time payment price ID

# ── Cloudflare Tunnel ─────────────────────────────────────────────────────────
# Create a tunnel at https://one.dash.cloudflare.com → Zero Trust → Access → Tunnels
# Point it to: http://frontend:80
CLOUDFLARE_TUNNEL_TOKEN=
